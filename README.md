# 내 공사 미리보기

반셀프·부분 인테리어 계획을 넣으면 공정 순서대로 빠진 것·꼬임·책임 공백·확인 필요를 짚어주는 사전 점검 도구.

- Stack: Vite + React + TypeScript + Tailwind v4 + shadcn/ui(수동 구성)
- Dev: `npm install && npm run dev`
- Build: `npm run build` → `dist/`
- Deploy: Netlify (netlify.toml 포함, publish=dist)
